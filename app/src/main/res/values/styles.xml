<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- TextView General -->
    <style name="TextViewStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <item name="typeface">roboto_regular</item>
    </style>

    <!-- TextView Small MotivApp-->
    <style name="TextViewStyle.Small">
        <item name="android:textSize">12sp</item>
    </style>

    <!-- TextView Medium MotivApp-->
    <style name="TextViewStyle.Medium">
        <item name="android:textSize">14sp</item>
    </style>

    <!-- TextView Large MotivApp-->
    <style name="TextViewStyle.Large">
        <item name="android:textSize">16sp</item>
    </style>

    <!-- TextView Large MotivApp-->
    <style name="TextViewStyle.Large.Bold">
        <item name="typeface">roboto_black</item>
    </style>

    <!-- TextView Large MotivApp-->
    <style name="TextViewStyle.Medium.Bold">
        <item name="typeface">roboto_black</item>
    </style>

    <!-- Tab TextAppearance Icon -->
    <style name="TabTextAppearanceIcon">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/tab_title</item>
        <item name="android:maxLines">1</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- TabLayout -->
    <style name="TabLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="tabIndicatorColor">@color/colorPrimary</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/TabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/grey_dark</item>
    </style>

    <!-- Tab TextAppearance -->
    <style name="TabTextAppearance">
        <item name="android:textSize">9sp</item>
        <item name="android:textColor">@color/grey_dark</item>
        <item name="android:maxLines">1</item>
        <item name="android:textAllCaps">true</item>
    </style>
</resources>
